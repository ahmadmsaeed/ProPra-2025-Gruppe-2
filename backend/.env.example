# Datenbank-URL für PostgreSQL
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:password@localhost:5432/sql_learning_platform?schema=public"

# JWT Secret für die Authentifizierung
# Für die Produktion einen sicheren zufälligen String verwenden
JWT_SECRET="super-secure-jwt-secret-for-authentication"

# Optional: Port für den Backend-Server (Standard: 3000)
# PORT=3000